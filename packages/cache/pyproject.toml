[project]
name = "contentos-cache"
version = "0.2.0"
requires-python = ">=3.12"
dependencies = [
    "redis>=5.0.0",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
