[project]
name = "contentos-ai-core"
version = "0.2.0"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.0",
    "openai>=1.58.0",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
