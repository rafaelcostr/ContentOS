apiVersion: 1

providers:
  - name: ContentOS
    orgId: 1
    folder: ContentOS
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
